#README 
# hashes a file, stores a baseline, detects tampering by comparing hashes
# File Integrity Monitoring; how SOCs catch modified system files, 
# backdoors, web shells; required by standards like PCI-DSS
# the baseline sits unprotected next to the file, so an attacker could overwrite it;
# real FIM stores baselines out of reach
